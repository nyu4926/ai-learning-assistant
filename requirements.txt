# ═══════════════════════════════════════════════════════════════
#  AI 学习助手 — 后端依赖
#  安装: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

# ── Web 框架 ──────────────────────────────────────────────────
flask>=3.0
flask-cors>=4.0

# ── LLM 调用（OpenAI 兼容接口，DeepSeek 等） ─────────────────
openai>=1.30.0

# ── 向量数据库 & Embedding ───────────────────────────────────
chromadb>=0.5.0
sentence-transformers>=3.0.0

# ── 文档解析 ─────────────────────────────────────────────────
PyMuPDF>=1.24.0          # PDF
python-pptx>=0.6.23      # PPTX / PPT
python-docx>=1.1.0       # DOCX / DOC

# ── 工具 ─────────────────────────────────────────────────────
# （sentence-transformers 会自动安装 torch, transformers, huggingface-hub 等）
# （chromadb 会自动安装 onnxruntime, pydantic 等）
