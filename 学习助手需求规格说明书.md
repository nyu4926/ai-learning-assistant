# 大模型课程学习助手 — 功能需求规格说明书

**版本：v1.0  |  日期：2026-05-01**

---

## 一、功能拆解与优先级

### 1.1 优先级定义

| 优先级 | 含义 | 阶段 |
|--------|------|------|
| **P0-必须** | MVP 必做，没有这些功能产品不可用 | Phase 1 |
| **P1-重要** | V1.1 迭代做，显著提升体验 | Phase 2 |
| **P2-可选** | V2.0 及以后考虑，锦上添花 | Phase 3 |

### 1.2 全量功能清单（共 18 个功能点）

#### F1 传资料

| 编号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| F1.1 | 上传资料（PDF/PPT/Markdown） | **P0** | 支持拖拽或点击上传，解析文件文本内容入库 |
| F1.2 | 资料列表管理 | **P0** | 展示已上传的资料列表，可删除、重新上传 |
| F1.3 | 资料预览 | P1 | 在线预览已上传的 PDF/PPT/Markdown 原文 |
| F1.4 | 资料分类/标签 | P2 | 给资料打标签（如：Transformer、RLHF），方便筛选 |

> **模块核心逻辑**：「文件上传 + 文本提取」。PDF 用 pdfplumber/pdf.js 提取，PPT 用 python-pptx 提取，Markdown 直接读。提取后分块存入数据库供后续检索。P0 先跑通上传和提取流程。

#### F2 聊资料

| 编号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| F2.1 | 基于资料的问答对话 | **P0** | 选择资料后，AI 仅基于该资料内容回答问题，引用原文出处 |
| F2.2 | 多轮对话上下文 | **P0** | 支持连续追问，保持上下文连贯 |
| F2.3 | 对话历史记录 | P1 | 保存历史对话，可回顾之前的问答 |
| F2.4 | 跨资料综合回答 | P2 | 同时选中多份资料，AI 综合所有资料内容回答 |

> **模块核心逻辑**：「RAG 检索增强生成」。用户提问 → 在已选资料的文本块中检索相关段落 → 把检索结果塞进 prompt 让 LLM 基于其回答。P0 必须做到回答有据可查（引用原文段落）。

#### F3 做测评

| 编号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| F3.1 | 自动生成选择题 | **P0** | 基于指定资料自动生成 4 选 1 选择题（单选） |
| F3.2 | 自动生成判断题 | **P0** | 基于指定资料自动生成判断题（对/错） |
| F3.3 | 自动生成简答题 | P1 | 基于指定资料自动生成开放性简答题 |
| F3.4 | 自定义出题参数 | P1 | 用户可选择出题范围（哪份资料）、题型组合、题目数量 |
| F3.5 | 答题与提交 | **P0** | 答题界面：选择题点选、判断题切换、简答题输入文字 |
| F3.6 | 自动批改 + 解析 | **P0** | 提交后 AI 自动批改，选择题/判断题即时判对错，简答题 0/0.5/1 分并给出评分理由 |

> **模块核心逻辑**：「出题 → 答题 → 批改」闭环。LLM 基于资料内容生成题目及标准答案，用户作答后 LLM 批改。选择题/判断题客观题直接匹配，简答题需要 LLM 打分（0/0.5/1 三档）。

#### F4 看进度

| 编号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| F4.1 | 知识点掌握度统计 | **P0** | 按资料维度统计各知识点的正确率，用进度条或雷达图呈现 |
| F4.2 | 薄弱知识点标记 | P1 | 正确率低于阈值的知识点标红，提示"建议复习" |
| F4.3 | 学习时长记录 | P2 | 记录每次学习/答题的时长，累计统计 |

> **模块核心逻辑**：从答题记录中统计每个知识点的掌握度。按资料维度聚合，用正确率衡量。P0 展示一个总览面板即可。

#### F5 出周报

| 编号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| F5.1 | 周报自动生成 | **P0** | 每周汇总：学了哪些资料、做了多少测评、掌握情况变化、下周推荐 |
| F5.2 | 周报导出 | P2 | 支持导出为图片或 PDF 格式 |

> **模块核心逻辑**：定期把分散的学习数据汇总成一份可读报告。LLM 根据本周原始数据（学了什么、测了什么、对了多少）生成自然语言总结 + 下周复习建议。

---

## 二、后台接口设计

### 接口规范

- 统一前缀：`/api/`
- 认证方式：预留 `Header Authorization: Bearer <token>`（V1 可先不做鉴权）
- 编码：全部 UTF-8，`Content-Type: application/json`
- 上传接口：`Content-Type: multipart/form-data`
- 分页：列表接口预留 `page/page_size` 参数
- 错误码统一格式：`{ code: int, message: string, data: null }`

### 接口清单（共 13 个）

#### 资料模块（4 个）

| 方法 | 地址 | 名称 | 请求参数 | 返回结构 |
|------|------|------|----------|----------|
| POST | `/api/materials/upload` | 上传资料 | `{ file: File, filename: string, tags?: string[] }` | `{ material_id, title, status }` |
| GET | `/api/materials/list` | 获取资料列表 | `{}` | `[{ id, title, type, tags[], upload_time, page_count, status }]` |
| GET | `/api/materials/:id` | 获取资料详情+全文 | path: `id` | `{ id, title, content: string, chunks[], metadata }` |
| DELETE | `/api/materials/:id` | 删除资料 | path: `id` | `{ success: boolean }` |

#### 对话模块（3 个）

| 方法 | 地址 | 名称 | 请求参数 | 返回结构 |
|------|------|------|----------|----------|
| POST | `/api/chat/send` | 发送消息 | `{ message: string, material_ids: string[], session_id?: string }` | `{ reply: string, sources: [{material_id, chunk_text}], session_id }` |
| GET | `/api/chat/history/:session_id` | 获取对话历史 | path: `session_id` | `[{ role:'user'\|'assistant', content, timestamp, sources? }]` |
| GET | `/api/chat/sessions` | 获取会话列表 | `{}` | `[{ session_id, title, last_time, material_count }]` |

#### 测评模块（3 个）

| 方法 | 地址 | 名称 | 请求参数 | 返回结构 |
|------|------|------|----------|----------|
| POST | `/api/quiz/generate` | 生成试卷 | `{ material_ids: string[], types: ('choice'\|'judge'\|'essay')[], counts: { choice?, judge?, essay? } }` | `{ quiz_id, questions: [{ id, type, question_text, options?[], answer, explanation }] }` |
| POST | `/api/quiz/submit` | 提交答卷 | `{ quiz_id: string, answers: [{ question_id, user_answer: string }] }` | `{ score: number, total: number, details: [{ question_id, correct, score, comment }] }` |
| GET | `/api/quiz/results` | 获取历史成绩 | `{ material_id?: string, limit?: number }` | `[{ quiz_id, date, score, total, question_count }]` |

#### 进度模块（2 个）

| 方法 | 地址 | 名称 | 请求参数 | 返回结构 |
|------|------|------|----------|----------|
| GET | `/api/progress/overview` | 进度总览 | `{}` | `{ materials_learned, quizzes_taken, accuracy_rate, weak_points[] }` |
| GET | `/api/progress/detail/:material_id` | 单份资料进度 | path: `material_id` | `{ material_id, mastery_percent, quiz_history[], knowledge_points[{name, accuracy}] }` |

#### 周报模块（1 个）

| 方法 | 地址 | 名称 | 请求参数 | 返回结构 |
|------|------|------|----------|----------|
| GET | `/api/weekly-report` | 获取本周报告 | `{ week_start_date?: string (ISO) }` | `{ week_range, summary, materials_studied[], quiz_stats, progress_delta, recommendations[] }` |

---

## 三、数据库表设计（共 7 张表）

### ER 关系概览

```
materials ──1:N──> chat_sessions ──1:N──> chat_messages
    │                    │
    │                    │
    1:N                  1:N
    ↓                    ↓
knowledge_progress   quizzes ──1:N──> quiz_attempts
                                            │
                                            │ 更新
                                            ↓
                                   knowledge_progress.accuracy
```

### 3.1 materials（资料表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | VARCHAR(36) PK | UUID 主键 |
| title | VARCHAR(255) NOT NULL | 资料标题（取自文件名） |
| file_type | ENUM('pdf','ppt','markdown') | 文件类型 |
| file_path | VARCHAR(512) | 存储路径 |
| content_text | TEXT | 提取后的全文纯文本 |
| chunks_json | JSON | 分块后的文本片段（用于 RAG 检索） |
| tags | JSON | 标签数组 |
| status | ENUM('uploading','parsing','ready','error') | 处理状态 |
| page_count | INT DEFAULT 0 | 页数 / 幻灯片数 |
| upload_time | DATETIME | 上传时间 |
| created_at / updated_at | DATETIME | 创建 / 更新时间 |

### 3.2 chat_sessions（会话表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | VARCHAR(36) PK | UUID 主键 |
| user_id | VARCHAR(36) | 用户 ID（预留多用户扩展） |
| title | VARCHAR(255) | 会话标题（取首条消息摘要） |
| material_ids | JSON | 本次会话关联的资料 ID 列表 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 最后更新时间 |

### 3.3 chat_messages（消息表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | VARCHAR(36) PK | UUID 主键 |
| session_id | VARCHAR(36) FK → chat_sessions.id | 所属会话 |
| role | ENUM('user','assistant') | 角色 |
| content | TEXT NOT NULL | 消息内容 |
| sources | JSON | AI 回复引用的资料来源 |
| created_at | DATETIME | 时间戳 |

### 3.4 quizzes（试卷表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | VARCHAR(36) PK | UUID 主键 |
| material_ids | JSON | 出题依据的资料 ID 列表 |
| questions_json | JSON | 题目数组（含类型、选项、答案、解析） |
| total_questions | INT | 题目总数 |
| created_at | DATETIME | 生成时间 |

### 3.5 quiz_attempts（答题记录表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | VARCHAR(36) PK | UUID 主键 |
| quiz_id | VARCHAR(36) FK → quizzes.id | 对应试卷 |
| answers_json | JSON | 用户的作答记录 |
| score | DECIMAL(5,2) | 得分 |
| total_score | DECIMAL(5,2) | 满分 |
| details_json | JSON | 每道题批改详情（是否正确、得分、评语） |
| completed_at | DATETIME | 完成时间 |

### 3.6 knowledge_progress（知识点进度表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | VARCHAR(36) PK | UUID 主键 |
| material_id | VARCHAR(36) FK → materials.id | 关联资料 |
| point_name | VARCHAR(255) NOT NULL | 知识点名称（如"注意力机制"） |
| total_asked | INT DEFAULT 0 | 该考点被考次数 |
| correct_count | INT DEFAULT 0 | 答对次数 |
| accuracy | DECIMAL(5,2) | 正确率（= correct_count / total_asked） |
| last_updated | DATETIME | 最后更新时间 |

### 3.7 weekly_reports（周报表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | VARCHAR(36) PK | UUID 主键 |
| week_start | DATE NOT NULL | 周起始日期 |
| week_end | DATE NOT NULL | 周结束日期 |
| report_data | JSON NOT NULL | 完整周报内容（含总结、统计数据、推荐等） |
| generated_at | DATETIME | 生成时间 |

### 3.8 关系说明

- **materials 1:N chat_sessions** — 一份资料可在多个会话中被引用
- **chat_sessions 1:N chat_messages** — 一个会话包含多条消息
- **quizzes 1:N quiz_attempts** — 一份试卷可被多次作答
- **materials 1:N knowledge_progress** — 一份资料包含多个追踪的知识点
- **quiz_attempts → knowledge_progress** — 每次答题后更新对应知识点的正确率

---

## 四、AI 系统提示词（人设）

### 4.1 日常聊天人设 — 「学习导师」模式

```
# 角色
你是一位资深 AI 产品经理转型的大模型课程导师。你的学员正在学习大模型相关的技术课程。

# 性格特征
- 说话有洞察力，不啰嗦，一针见血
- 能把复杂概念用简单类比讲清楚
- 会主动指出学员理解中的常见误区
- 语气像一位经验丰富的同事在带新人，不端着不装

# 核心规则（最高优先级）
1. **严格基于提供的资料内容回答**。你只拥有以下资料作为知识来源：
{{MATERIAL_CONTEXT}}
2. 如果问题超出资料范围，明确告知："这个问题超出了当前资料覆盖的范围，
   建议补充 XX 方面的资料。"
3. **每次回答必须标注引用来源**，格式为：
   > 📖 引自《资料名称》：原文摘录...
4. 不要编造资料中没有的内容，即使你对这个话题很了解也不行。

# 回答风格
- 开门见山，先给结论再解释原因
- 用类比和实例帮助理解，避免堆砌术语
- 当学员的理解有明显偏差时，温和但坚定地纠正
- 适当反问，引导学员自己思考（"你觉得这里为什么这样设计？"）

# 禁止行为
❌ 不要说"这是一个很好的问题"之类废话
❌ 不要长篇大论，控制在 300 字以内（除非学员要求展开）
❌ 不要用外部知识替代资料内容回答
❌ 不要扮演客服语气
```

---

### 4.2 出题人设 — 「严谨命题官」模式

```
# 角色
你是一位严格的考试命题专家，专门负责根据学习资料编写测试题目。

# 任务
基于以下资料内容，生成一套测试题。
{{MATERIAL_CONTEXT}}

# 用户参数（从请求中读取）
- 题型要求：{{QUIZ_TYPES}}（choice=选择题, judge=判断题, essay=简答题）
- 数量要求：{{QUIZ_COUNTS}}
- 目标难度：中等偏难（区分"背过"和"真正理解"的人）

# 输出格式（严格 JSON）
{
  "questions": [
    {
      "id": "Q1",
      "type": "choice | judge | essay",
      "question": "题干文字",
      "options": ["A.xxx","B.xxx","C.xxx","D.xxx"],   // 仅 choice 类型需要
      "answer": "标准答案（choice写选项字母，judge写对/错，essay写参考要点）",
      "explanation": "解题思路/解析",
      "source_material": "出自哪份资料哪个章节"
    }
  ]
}

# 命题规则

## 选择题规则
- 必须 4 选 1 单选，只有一个正确答案
- 干扰项要有迷惑性（常见错误理解、易混淆概念）
- 不能出现"以上都对""以上都错"等偷懒选项
- 题干不能直接照抄原文句子，要换一种问法

## 判断题规则
- 陈述句，判断对或错
- 要考察容易搞混的概念（如"Attention机制只用于Encoder"→错）
- 正确和错误的比例大致 5:5

## 简答题规则
- 问题要具体，不要泛泛而谈（坏例子："请谈谈Transformer"）
- 参考答案要列出关键得分点（2-3个要点即可）
- 答案能在资料中找到明确依据

# 质量检查（输出前自查）
✅ 所有题目都能在给定资料中找到答案依据
✅ 没有一道题是靠常识就能答对的
✅ 题目覆盖了资料的不同章节/知识点，不扎堆
✅ 选择题的干扰项确实能迷惑没学懂的人
```

---

### 4.3 改卷人设 — 「公正阅卷官」模式

```
# 角色
你是一位公正严格的阅卷老师，负责批改学员的测验答卷。

# 输入信息
- 原始题目及标准答案：
{{QUESTIONS_WITH_ANSWERS}}
- 学员的作答：
{{USER_ANSWERS}}
- 对应资料原文（作为判分依据）：
{{MATERIAL_CONTEXT}}

# 输出格式（严格 JSON）
{
  "summary": {
    "total_score": 12.0,
    "max_score": 15.0,
    "accuracy": "80%"
  },
  "details": [
    {
      "question_id": "Q1",
      "type": "choice|judge|essay",
      "user_answer": "学员答案",
      "correct_answer": "标准答案",
      "is_correct": true/false,
      "score": 1,           // 客观题固定1分；主观题 0/0.5/1
      "comment": "评语"
    }
  ],
  "feedback": "整体评价和学习建议（2-3句话）"
}

# 批改规则

## 客观题（选择 + 判断）
- **严格匹配**：学员答案与标准答案完全一致
  → is_correct=true, score=1, comment="✅ 正确"
- 不一致
  → is_correct=false, score=0, comment="❌ 错误。正确答案是 X。原因：..."

## 主观题（简答题）— 三档评分制
┌──────────┬────────────────────────────────────────────┐
│   得分   │                   标准                      │
├──────────┼────────────────────────────────────────────┤
│  **1分**  │ 涵盖了所有关键要点，表述清晰准确，             │
│ (完全正确)│ 与参考答案的核心意思一致                     │
├──────────┼────────────────────────────────────────────┤
│ **0.5分**  │ 答对了一部分但遗漏了关键点，                 │
│ (部分正确)│ 或者方向对但表述不够准确；给了思路但不完整     │
├──────────┼────────────────────────────────────────────┤
│  **0分**  │ 完全偏离题意，答非所问；                      │
│ (错误)   │ 或者只写了无关内容                            │
└──────────┴────────────────────────────────────────────┘

### 简答题评分细则
- 先对照标准答案的关键得分点（通常 2-3 个）
- 全部命中 → 1 分
- 命中部分（≥1个但未全中）→ 0.5 分
- 未命中任何关键点 → 0 分
- 如果学员的回答虽然措辞不同但意思完全正确，应给 1 分（不抠字眼）

# 评语风格
- ✅ 正确时：简洁确认，可以补充一句知识拓展
- ❌ 错误时：指出错误原因，引导回资料中的正确描述
- 📝 简答题半对时：列出哪些说对了、哪些漏掉了

# 整体反馈（feedback 字段）
- 总结本次测验表现（哪些掌握好、哪些薄弱）
- 给出 2 条具体的改进建议
- 语气鼓励但诚实，不吹捧
```

---

## 五、开发排期建议

| 阶段 | 内容 | 涉及功能 |
|------|------|----------|
| **Phase 1（MVP）** | 实现所有 P0 功能 | F1.1-F1.2, F2.1-F2.2, F3.1-F3.2/F3.5-F3.6, F4.1, F5.1 |
| **Phase 2（V1.1）** | 补齐 P1 功能 | F1.3, F2.3, F3.3-F3.4, F4.2 |
| **Phase 3（V2.0）** | P2 功能 | F1.4, F2.4, F4.3, F5.2 |

---

*文档结束*
